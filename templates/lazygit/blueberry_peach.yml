gui:
  theme:
    activeBorderColor:
      - "{{violet}}"
      - bold
    authorColors:
      "*": "{{violet}}"
    cherryPickedCommitBgColor:
      - "{{surface1}}"
    cherryPickedCommitFgColor:
      - "{{violet}}"
    defaultFgColor:
      - "{{text}}"
    inactiveBorderColor:
      - "{{subtext}}"
    optionsTextColor:
      - "{{blue}}"
    searchingActiveBorderColor:
      - "{{yellow}}"
    selectedLineBgColor:
      - "{{surface0}}"
    unstagedChangesColor:
      - "{{red}}"
